# History

## 2026-07-10 — старт проекта

- Проверен Figma frame `1:1100`.
- Зафиксированы desktop-размеры `1600×8002` и контейнер `1200 px`.
- Зафиксированы основные шрифтовые токены: Cygre ExtraBold для заголовков и Inter для основного текста.
- Создана минимальная архитектура под последовательную секционную верстку.
- Сборка и локальная раздача `index.html`, CSS и JS проверены с HTTP-ответом `200`.

## 2026-07-10 — header и hero

- Скачаны локальные шрифты и Figma-ассеты для первого экрана.
- Семантический `h1` оставлен текстовым; декоративные изображения вложены в него с `alt=""`.
- Header и hero собраны по desktop-токенам Figma.
- Добавлено полноэкранное меню для tablet/mobile с закрытием по кнопке, Escape и ссылкам.
- Уточнена геометрия hero по MCP: внутренний offset `114 px`, stats `top: 480 px`, description `left: 465 px`, actions `left: 114 px`, межстрочные смещения заголовка `15/8 px`.
- Сверстана секция проблем: реальные иконки из Figma, карточки `384 px`, gaps `24/32 px`, центрирование неполной последней строки.
- Зафиксирован общий вертикальный ритм секций: `padding-top: 90 px` и `padding-bottom: 90 px`.
- Fluid-типографика и адаптивные значения вынесены в `static/scss/base/_mixins.scss`; применены к заголовкам, карточкам, hero и section spacing.
- Сверстан блок «План действий в цифрах» с сеткой `384/464/384`, пятью именованными SVG-slot-областями и адаптацией до одной колонки.
- Временные CSS-декорации карточек метрик удалены; оставлены только цвет, текст и пустые SVG-slot-слои.
- Tablet/mobile layout metrics переведён в одну колонку: карточки идут последовательно и занимают всю ширину родителя.
- SVG-файлы `metrics-*-art.svg` подключены в Pug-компонент автоматически по имени slot.
- Сверстаны три карточки «Три шага до предложения о работе»: fluid-height, адаптивная колонка и SVG-ассеты из Figma.
- Нижние SVG-цифры карточек смещены вниз на `46 px` и обрезаются контейнером, как в Figma; body-текст карточек переведён в серый цвет.
- Добавлен отдельный план будущей scroll-reveal/stagger-анимации для всех секций и элементов.
